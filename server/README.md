# Servidor

| Pasta | Uso |
|---|---|
| `wyd-go/` | Código Go (protocolo 7.48) — build amd64 ou arm64 |
| `deploy/arm64/` | Tree de deploy ARM64 (Docker Compose, `data/` de exemplo) |

Skills: `../SKILLS/server/wyd-server-go-arm64`, `wyd-server-docker-deploy`.

Troque `admin_password` / `WYD_ADMIN_PASSWORD` antes de subir em produção.
